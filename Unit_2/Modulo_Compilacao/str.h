/*Funcoes oferecidas pelo modulo str.c */

/*Funcao comprimento
**Retorna o numero de caracteres da string passada como parametro
*/
int comprimento (char *str);

/*Funcao copia
**Copia os caracteres da string oring (origem) para dest (destino)
*/
void copia (char *dest, char *orig);

/*Funcao concatena
**Concatena a string orig (origem) na string dest (destino)
*/
void concatena (char *dest, char *orig);
